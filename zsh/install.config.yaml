# Dotbot config file

- defaults:
    link:
      relink: true
    shell:
      stdout: true
      stderr: true

- link:
    ~/.zshrc: zshrc
    ~/.oh-my-zsh/custom.symlink: ohmyzsh-custom
    ~/.p10k.zsh: p10k.zsh

- shell:
    - git submodule update --depth 1
